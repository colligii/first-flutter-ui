import 'package:flutter/material.dart';
import 'routes/UI.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter UI Home',
      home: UI(),
    );
  }
}
